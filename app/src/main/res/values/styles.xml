<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ImagePickerAppTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/yo_bar_color</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">@color/colorPrimary</item>

        <item name="toolbarStyle">@style/MyToolbarStyle</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/yo_bg_app</item>
    </style>

    <style name="MyToolbarStyle" parent="Base.Widget.AppCompat.Toolbar">
        <item name="titleTextAppearance">@style/Toolbar_TextAppearance_Dark</item>
    </style>

    <style name="Toolbar_TextAppearance_Dark">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/yo_bar_title_color</item>
    </style>
</resources>
